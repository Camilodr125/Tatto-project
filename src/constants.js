/** Studio contact email — used in UI, mailto links, and README */
export const STUDIO_EMAIL = 'sadekyt8@gmail.com'

/**
 * Brand mark — place your file at `public/logo.png`.
 * Used for the browser tab icon, header, and footer.
 */
export const STUDIO_LOGO_SRC = '/logo.png'

/** Physical address — footer, booking sidebar */
export const STUDIO_ADDRESS = {
  line1: '4101 Bryan Avenue, Suite 120',
  line2: 'Dallas, Texas 75204',
}

/** Google Maps search for the studio (open in new tab from footer) */
export const STUDIO_MAPS_URL =
  'https://www.google.com/maps/search/?api=1&query=4101+Bryan+Ave+Suite+120%2C+Dallas%2C+TX+75204'

/** Merch page + nav links — set `true` when the line is ready to show publicly */
export const FEATURE_MERCH = false
